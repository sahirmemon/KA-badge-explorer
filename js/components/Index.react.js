var React = require('react');

var Index = React.createClass({
  render: function () {
    return (
      <div className="blank">
        <p>Choose a badge type from the left.</p>
      </div>
    );
  }
});

module.exports = Index;